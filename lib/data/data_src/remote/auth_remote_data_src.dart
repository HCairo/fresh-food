class AuthRemoteDataSrc {
  // Implémenter les méthodes pour gérer les données d'authentification à distance
  Future<void> login(String email, String password) async {
    // Appeler l'API ou Firebase pour la connexion
  }

  Future<void> register(String email, String password) async {
    // Appeler l'API ou Firebase pour l'enregistrement
  }
}
